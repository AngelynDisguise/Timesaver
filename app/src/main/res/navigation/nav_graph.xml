<?xml version="1.0" encoding="utf-8"?>
<navigation
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/main_fragment">

    <fragment
        android:id="@+id/main_fragment"
        android:name="com.example.timesaver.MainFragment"
        tools:layout="@layout/fragment_main"
        android:label="Timesaver">

        <action
            android:id="@+id/action_main_to_settings"
            app:destination="@id/settings_fragment" />
        <action
            android:id="@+id/action_main_to_activities_fragment"
            app:destination="@id/activities_fragment" />
    </fragment>

    <fragment
        android:id="@+id/settings_fragment"
        android:name="com.example.timesaver.SettingsFragment"
        tools:layout="@layout/fragment_settings"
        android:label="Settings" />

    <fragment
        android:id="@+id/activities_fragment"
        android:name="com.example.timesaver.ActivitiesFragment"
        tools:layout="@layout/fragment_activities"
        android:label="Activities" />

</navigation>